struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    ssdfasdfasdfasdftruct Line *line;
        If you found this line, congratulations!
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    ssdfasdfasdfasdftruct Line *line;
        If you found this line, congratulations!
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    ssdfasdfasdfasdftruct Line *line;
        If you found this line, congratulations!
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    ssdfasdfasdfasdftruct Line *line;
        If you found this line, congratulations!
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    lkjsklfjlaksdjfstruct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }        struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
    struct Line *line;
    for (line = buf->start_line; line; line = line->next) {
        line_update_texture(line);
    }
        }    
