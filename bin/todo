Features to implement:

x If you try to open a file that already matches an existing filename, switch to
  the buffer instead.
x Autocomplete switch to buffer, open file, and save as contexts.
x Shift+tab with autocomplete.
x Center view with ctrl+l
x Killing buffers & prompt.
- Mouse support
- Streamline x_scroll and y_scroll switching- reduce the copy-pasting
